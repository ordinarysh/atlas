/**
 * PostCSS Configuration for Tailwind CSS v4
 *
 * Professional monorepo setup following v4 best practices.
 * v4 automatically handles postcss-import and autoprefixer.
 */
export default {
  plugins: {
    // Simple v4 PostCSS plugin - all options are handled via CSS imports
    '@tailwindcss/postcss': {},
  },
}
