#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Skip on CI
if [ "$CI" = "true" ]; then
  echo "⏭️  Skipping commit-msg hooks in CI environment"
  exit 0
fi

npx --no-install commitlint --edit "$1"