#!/bin/sh
set -e

STAGED="$(git diff --cached --name-only --diff-filter=ACMR)"
[ -z "$STAGED" ] && exit 0

echo "$STAGED" | grep -qv '^templates/' || exit 0

# Force root config so it never discovers template configs
pnpm exec lint-staged --config ./.lintstagedrc.mjs